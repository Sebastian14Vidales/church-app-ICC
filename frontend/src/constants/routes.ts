export const PATHS = {
  dashboard: "/",
  courses: "/courses",
  members: "/members",
  events: "/events",
  offerings: "/offerings",
  lifeGroups: "/life-groups",
  reports: "/reports",
} as const;

export default PATHS;